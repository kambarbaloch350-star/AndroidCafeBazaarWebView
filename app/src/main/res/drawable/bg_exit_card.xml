<?xml version="1.0" encoding="utf-8"?>
<!-- Light green card of the exit confirmation dialog. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="26dp" />
    <gradient
        android:angle="270"
        android:endColor="@color/exit_surface_end"
        android:startColor="@color/exit_surface" />
    <stroke
        android:width="1.5dp"
        android:color="@color/exit_border" />
</shape>
